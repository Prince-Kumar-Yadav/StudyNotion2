<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Registration Page</title>
    <style>

        .container5 {
            display: flex;
            margin: auto;
            max-width: 1200px;
            width: 90%;
            background-color: #161b22;
            border-radius: 8px;
            overflow: hidden;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
        }

        .form-section {
            flex: 1;
            padding: 40px;
        }

            .form-section h1 {
                font-size: 24px;
                margin-bottom: 10px;
            }

            .form-section p {
                font-size: 14px;
                margin-bottom: 30px;
                color: #9ca3af;
            }

                .form-section p a {
                    color: #58a6ff;
                    text-decoration: none;
                }

                    .form-section p a:hover {
                        text-decoration: underline;
                    }

        .toggle-buttons {
            display: flex;
            margin-bottom: 20px;
        }

            .toggle-buttons button {
                flex: 1;
                padding: 12px;
                font-size: 14px;
                color: #fff;
                background-color: #30363d;
                border: none;
                border-radius: 4px;
                cursor: pointer;
                margin-right: 8px;
            }

                .toggle-buttons button:last-child {
                    margin-right: 0;
                }

                .toggle-buttons button.active {
                    background-color: #fbbc05;
                    color: #000;
                }

        .form-group {
            display: flex;
            gap: 20px;
            margin-bottom: 20px;
        }

            .form-group input {
                width: 100%;
                padding: 12px;
                font-size: 14px;
                border: 1px solid #30363d;
                border-radius: 4px;
                background-color: #0d1117;
                color: #fff;
            }

                .form-group input:focus {
                    outline: none;
                    border-color: #58a6ff;
                }

        .password-toggle {
            position: relative;
        }

            .password-toggle i {
                position: absolute;
                top: 50%;
                right: 10px;
                transform: translateY(-50%);
                cursor: pointer;
                color: #9ca3af;
            }

        .button {
            display: block;
            width: 100%;
            padding: 12px;
            font-size: 16px;
            font-weight: bold;
            text-align: center;
            background-color: #fbbc05;
            color: #000;
            border: none;
            border-radius: 4px;
            cursor: pointer;
            margin-top: 20px;
        }
            .button:hover {
                background-color: #e0aa04;
            }

        .image-section {
            flex: 1;
            margin-top: 50px;
            background: url('https://via.placeholder.com/600') no-repeat center/cover;
        }
    </style>
</head>
<body>
    <div class="container5">
        <div class="form-section">
            <h1>Join the millions learning to code with StudyNotion for free</h1>
            <p>Build skills for today, tomorrow, and beyond. <a href="#">Education to future-proof your career.</a></p>
            <div class="toggle-buttons">
                <button class="active">Student</button>
                <button>Instructor</button>
            </div>
            <form>
                <div class="form-group">
                    <input type="text" placeholder="First Name *" required>
                    <input type="text" placeholder="Last Name *" required>
                </div>
                <div class="form-group">
                    <input type="email" placeholder="Email Address *" required>
                </div>
                <div class="form-group">
                    <input type="tel" placeholder="Phone Number">
                </div>
                <div class="form-group">
                    <div style="position: relative; flex: 1;">
                        <input type="password" id="password" placeholder="Password *" required>
                        <i onclick="togglePassword()" style="position: absolute; right: 10px; top: 50%; transform: translateY(-50%); cursor: pointer;">👁️</i>
                    </div>
                    <div style="position: relative; flex: 1;">
                        <input type="password" id="confirm-password" placeholder="Confirm Password *" required>
                        <i onclick="togglePassword()" style="position: absolute; right: 10px; top: 50%; transform: translateY(-50%); cursor: pointer;">👁️</i>
                    </div>
                </div>
                <button type="submit" class="button">Create Account</button>
            </form>
        </div>
        <div class="image-section">
            <img src="https://my-study-notion.vercel.app/static/media/signup.acaf50bcb11d9aec44b4.webp" />
        </div>
    </div>

    <script>
        function togglePassword() {
          const passwordField = document.getElementById('password');
          const confirmPasswordField = document.getElementById('confirm-password');
          const type = passwordField.type === 'password' ? 'text' : 'password';
          passwordField.type = type;
          confirmPasswordField.type = type;
        }
    </script>
</body>
</html>
